#include "main.h"

/**
 * _abs - Entry point
 * @n: n is an interger
 * Description: e dey print value of integer sha
 * Return: int
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
